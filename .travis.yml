language: objective-c
osx_image: xcode11.3

before_install:
   - gem install xcpretty
before_script:
   - set -o pipefail


script:
   - cd class-dump && ls -la
   - mkdir out64
   - sed -i "" -e "s/com.apple.product-type.tool/com.apple.product-type.library.dynamic/g" class-dump.xcodeproj/project.pbxproj
   #- xcodebuild -project class-dump.xcodeproj -configuration Release -sdk iphoneos13.2 -arch arm64 PRODUCT_BUNDLE_IDENTIFIER="com.okanon.classdump" ONLY_ACTIVE_ARCH=NO build TARGET_BUILD_DIR="/User/traivs/build/okanon/class-dump.build/class-dump/out64"
   - xcodebuild -project class-dump.xcodeproj -target class-dump build -configuration Release -sdk iphoneos13.2 -arch arm64 IPHONEOS_DEPLOYMENT_TARGET=13.2
